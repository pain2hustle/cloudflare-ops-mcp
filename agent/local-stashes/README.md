# Local private stashes

Console exports may be saved here manually. Everything in this directory except this README is ignored by Git.

Use the site/date/time naming rule. Treat exports as private even though they are redacted; delete them when no longer needed.
